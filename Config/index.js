require("dotenv").config({ path: "./config.env" });

export const {
  PORT,
  DB_URL,
  DEBUG_MODE,
  JWT_SECRET,
  JWT_EXPIRE,
  COOKIE_EXPIRE,
  SMTP_MAIL,
  SMTP_SERVICE,
  SMTP_HOST,
  SMTP_PORT,
  SMTP_PASS,
  SMS_API_KEY,
  FRONTEND_URL,
  AWS_BUCKET,
  AWS_ACCESS_KEY,
  AWS_SECRET_ACCESS_KEY,
  AWS_REGION,
  CLOUD_NAME,
  CLOUD_API_KEY,
  CLOUD_API_SECRET,
  CLOUDINARY_URL,
  VONAGE_API_KEY,
  VONAGE_API_SECRET,
  TWILIO_PHONE_NUMBER,
  TWILIO_AUTH_SID,
  TWILIO_AUTH_TOKEN,
} = process.env;
